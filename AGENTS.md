Always create a new git worktree when working on a new feature.

When commit, both commit to the worktree's feature branch and merge all commits to the main branch. Auto resolve conflict by yourself, verify all tests pass.
